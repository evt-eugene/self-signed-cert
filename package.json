{
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build-forge": "node build-forge.mjs"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.2",
    "esbuild": "^0.20.0",
    "html-minifier-terser": "^7.2.0"
  }
}
